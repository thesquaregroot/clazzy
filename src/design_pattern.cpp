
#include "h/design_pattern.h"
using namespace clazzy;
using namespace std;

map<string, design_pattern> design_pattern_map = {
    {"SINGLETON", SINGLETON},
    {"MULTITON", MULTITON},
    {"FACTORY_METHOD", FACTORY_METHOD},
    {"ABSTRACT_FACTORY", ABSTRACT_FACTORY}
};

