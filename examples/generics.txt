
%LANGUAGE=C++,Java,Python;

A piece_of_bacon
    is a
        list<integer>,
        @singleton;
    can
        void smell_delicious,
        string get_name,
        piece_of_bacon get_strip(integer i);
    and has
        static map<integer,piece_of_bacon> strips,
        integer current_index.

