%LANGUAGES=Java,C++;

A person
    can
        jump
        talk(to)
        walk(distance)
        run(distance, speed)
        sprint(distance),
    has
        left_arm
        right_arm
        left_leg
        right_leg
.

A nagger is a person that
    can
        nag(person)
.
