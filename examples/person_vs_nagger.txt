%LANGUAGE=Java,C++,C;

A person
    can
        void jump,
        void talk(person to),
        void walk(integer distance),
        void run(integer distance, integer speed),
        void sprint(integer distance);
    has
        integer left_arm,
        integer right_arm,
        float left_leg,
        float right_leg.

A nagger is a person that
    can
        void nag(person p).
